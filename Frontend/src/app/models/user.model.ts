export interface User {
	id: string;
	name: string;
	address: string;
	credential: string;
	accessToken: string;
	refreshToken: string;
	opaqueToke: string;
	role: string;
	email: string;
	phone: string;
}

